image: current-alpine3.11

pages:
  cache:
    paths:
      - node_modules/

  script:
    - npm i firebase-tools -g
    - cd website
    - npm install
    - npm run build
    - cd ..
    - firebase deploy --token $FIREBASE_TOKEN
  only:
    - master
